<h1>You can create a dose here</h1>

<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.association :ingredient %>
    <%= f.input :description %>

    <%= f.submit "submit", class: "btn btn-warning" %>
  <% end %>
</div>
